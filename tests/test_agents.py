"""Unit tests for individual agents."""
import pytest

# TODO: Add tests for each agent function
# Example structure:
#
# from trip_planner.agents.flight_agent import flight_agent
#
# def test_flight_agent_returns_results():
#     mock_state = {
#         "user_query": "Trip to Tokyo for 7 days",
#         "trip_constraints": {"destination": "Tokyo", "budget": 2000},
#         "llm_calls": 0,
#     }
#     result = flight_agent(mock_state)
#     assert "flight_results" in result
